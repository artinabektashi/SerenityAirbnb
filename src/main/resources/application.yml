spring:
  datasource:
    url: jdbc:mysql://localhost:3306/serenity_db
    username: root
    password: mysqladmin
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8081

